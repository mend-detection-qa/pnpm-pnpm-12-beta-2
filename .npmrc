# Lock the supported architecture to Linux x64 so platform-filtered
# optional deps are deterministic regardless of the machine pnpm runs
# on. This mirrors the target UA scanning environment.
supported-architectures[os][]=linux
supported-architectures[cpu][]=x64
supported-architectures[libc][]=glibc
